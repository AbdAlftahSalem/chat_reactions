import 'package:flutter/widgets.dart';

class MenuItem {
  final Widget label;
  final Widget icon;
  final String value;

  // contsructor
  const MenuItem({
    required this.label,
    required this.icon,
    required this.value,
  });
}
